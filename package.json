{
  "name": "fret",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.21.1"
  },
  "// note": "server-only deps for api/* — install with `npm i web-push @vercel/kv` in /api or hoist here",
  "optionalDependencies": {
    "web-push": "^3.6.7",
    "@vercel/kv": "^3.0.0"
  }
}
